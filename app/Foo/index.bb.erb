<div data-role="page">

  <div data-role="header" data-position="inline">
    <%= link_to "New", :action => :new %>
    <h1>Foos</h1>
    <%= link_to "Home", RhoConfig::start_path %>
  </div>

  <div data-role="content">
    <table>
      <tr>
      <% @foos.each do |obj| %>
        
            <td class="recordLabel"><%= link_to "#{obj.bar1}", :action => :show, :id => obj.object %></td>
        
      <% end %>
      </tr>
    </table>
  </div>

</div>
